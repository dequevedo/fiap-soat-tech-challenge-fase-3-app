apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-app-config
  labels:
    app: {{ .Release.Name }}-app

data:
  SPRING_DATASOURCE_ENDPOINT: jdbc:postgresql://$(SPRING_DATASOURCE_ENDPOINT):5432/postgres