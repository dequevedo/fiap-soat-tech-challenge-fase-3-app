apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-app-config
  labels:
    app: {{ .Release.Name }}-app

data:
  SPRING_DATASOURCE_URL: jdbc:postgresql://{{ .Values.secrets.app.TF_VAR_DB_ENDPOINT }}:5432/postgres